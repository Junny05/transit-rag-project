services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: transit_admin
      POSTGRES_PASSWORD: transit_rag
      POSTGRES_DB: transit_alerts
    ports:
      - "5432:5432"
    volumes:
      - "pgdata:/var/lib/postgresql/data"

volumes:
  pgdata:
